from setuptools import setup, find_packages
#from distutils.core import setup

setup(
    name='asbeam',
    version='0',
    packages=[
        'asbeam',
    ],
    install_requires=[
        'csdl',
    ],
)